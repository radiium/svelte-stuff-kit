/**
 * A tiny (234B) utility for constructing className strings conditionally.
 * Credit: https://github.com/lukeed/clsx
 *
 * @param rest params
 * @returns
 */
export declare function clsx(...rest: unknown[]): string;
